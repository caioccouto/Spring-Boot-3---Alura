spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/vollmed
    username: root
    password: QualquersenhaH1I&
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true